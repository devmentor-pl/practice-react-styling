import React from 'react';
import CardImage from './CardImage'
import CardBody from './CardBody'
import StyledCard from './Card.styled'

const Card = (props) => {

    return <StyledCard>MARTIN</StyledCard>;
}

Card.Img = CardImage;
Card.Body = CardBody;

export default Card;
